services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_USE_MOCK: "false"
        VITE_API_BASE_URL: "http://localhost:8000/api/v1"
    ports:
      - "8080:80"
    restart: unless-stopped
